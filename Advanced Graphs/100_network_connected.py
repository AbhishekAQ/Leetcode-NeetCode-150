"""
Network Connected

You are given a network of n computers numbered from 0 to n-1 and a list of connections where each connection is a pair of computers. Return the minimum number of operations required to connect all the computers. If it is not possible, return -1.

Example:
Input: n = 4, connections = [[0,1],[0,2],[1,2]]
Output: 1

Constraints:
1 <= n <= 10^5
connections.length <= 10^5
connections[i].length == 2
0 <= ai, bi < n
ai != bi
"""
# Solution goes below
